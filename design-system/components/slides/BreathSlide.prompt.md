The one-beat pacing slide dropped between two revelations — no tag, no header, no media.

```jsx
<BreathSlide quote="Sauf que non." quoteAccent="non." sig="var(--sig-cejourla)" />
```

Keep the quote to 3–6 words. Never give it a tag, media, or pagination — that's what makes it read as a breath, not another fact slide.
