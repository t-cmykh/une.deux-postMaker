export interface BrandHeaderProps {
  /** true = cream ring/wordmark for dark slide grounds; false = ink for cream grounds. Default true. */
  dark?: boolean;
  /** Monogram text inside the ring. Default "1·2". */
  ring?: string;
  /** Handle shown right-aligned. Default "@UNE.DEUX". */
  handle?: string;
}
