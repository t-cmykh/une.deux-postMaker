The final carousel slide — one clear action, no new information.

```jsx
<CTASlide title="RESTE DANS LE JEU" body="Le foot en deux touches, tous les jours." btnlabel="FOLLOW" sig="var(--sig-cejourla)" onSig="var(--on-sig-cejourla)" />
```

Rule from the source SKILL: the button label is always "FOLLOW" in production — never propose a second action. `ENREGISTRE` only appears as the tool's own placeholder default.
