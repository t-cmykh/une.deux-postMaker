The fixed masthead (ring monogram + wordmark + handle + hairline) every une·deux slide opens with.

```jsx
<BrandHeader dark handle="@UNE.DEUX" />
```

- `dark`: flip to `false` on the cream-background CTA/quote templates.
- Always sits at the very top of a slide, before the series tag.
