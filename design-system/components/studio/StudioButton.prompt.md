The Studio panel's two footer actions — export and reset.

```jsx
<div style={{display:'flex',gap:10}}>
  <StudioButton>Exporter PNG</StudioButton>
  <StudioButton variant="ghost">Réinit.</StudioButton>
</div>
```
