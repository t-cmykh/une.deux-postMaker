One card in the 2×2 series-picker grid at the top of the Studio panel.

```jsx
<SerieButton name="Ce jour-là" description="Éphéméride · carrousel" swatch="var(--sig-cejourla)" active onClick={...} />
```

Selecting a series locks its color, photo grade, and aspect ratio everywhere downstream.
